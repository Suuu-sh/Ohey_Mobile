name: CI

on:
  push:
    branches:
      - development
    paths:
      - 'lib/**'
      - 'test/**'
      - 'assets/**'
      - 'config/firebase/**'
      - 'android/**'
      - 'ios/**'
      - 'scripts/**'
      - '.env.example'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'analysis_options.yaml'
      - '.github/workflows/ci.yml'
  pull_request:
    branches:
      - development
    paths:
      - 'lib/**'
      - 'test/**'
      - 'assets/**'
      - 'config/firebase/**'
      - 'android/**'
      - 'ios/**'
      - 'scripts/**'
      - '.env.example'
      - 'pubspec.yaml'
      - 'pubspec.lock'
      - 'analysis_options.yaml'
      - '.github/workflows/ci.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ohey-mobile-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  flutter_analyze:
    name: Flutter analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Run shared Flutter analyze action
        uses: Suuu-sh/Shared/actions/flutter-analyze@7f6f9dc12a0b3e34bee54aaf7c60f45dadfb6ba4
        with:
          working-directory: .
          flutter-channel: stable
          env-file-content: |
            OHEY_ENV=dev
            CLERK_PUBLISHABLE_KEY=test_clerk_publishable_key
            OHEY_AUTH_REDIRECT_URL=app.ohey.com.dev://login-callback/
            OHEY_BACKEND_URL=https://dev-ohey-backend.onrender.com
          analyze-command: flutter analyze --no-fatal-infos

  flutter_test:
    name: Flutter test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - name: Run shared Flutter test action
        uses: Suuu-sh/Shared/actions/flutter-test@7f6f9dc12a0b3e34bee54aaf7c60f45dadfb6ba4
        with:
          working-directory: .
          flutter-channel: stable
          env-file-content: |
            OHEY_ENV=dev
            CLERK_PUBLISHABLE_KEY=test_clerk_publishable_key
            OHEY_AUTH_REDIRECT_URL=app.ohey.com.dev://login-callback/
            OHEY_BACKEND_URL=https://dev-ohey-backend.onrender.com
          test-command: flutter test
