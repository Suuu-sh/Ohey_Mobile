# Nomo Character Assets

The app uses standalone transparent PNG files placed directly under:

`assets/characters/nomo/`

Only runtime assets live there. Do not add sprite sheets or generated preview folders to the app asset tree.
